# Release Docs

This folder contains packaging and release procedure docs.

- `build-and-package.md`: how `.ankiaddon` files are produced.
- `process.md`: end-to-end release sequence across local checks, GitHub, and AnkiWeb.
- `versioning.md`: version files, bump scripts, metadata sync, and tag rules.
